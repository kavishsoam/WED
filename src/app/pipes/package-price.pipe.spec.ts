import { PackagePricePipe } from './package-price.pipe';

describe('PackagePricePipe', () => {
  it('create an instance', () => {
    const pipe = new PackagePricePipe();
    expect(pipe).toBeTruthy();
  });
});
